# Nextcloud admin credentials
NEXTCLOUD_ADMIN_USER=your_admin_username
NEXTCLOUD_ADMIN_PASSWORD=your_admin_password

# Nextcloud data directory (used by manage_volume.sh -v flag)
NEXTCLOUD_DATA_DIR=/var/nextcloud_data

# Port to expose Nextcloud on the host (default 8080)
PORT=8080

# Public domain for Cloudflare / external access
OWN_DOMAIN=your-tunnel.example.com

# Optional extra trusted domains (space-separated). OWN_DOMAIN is added automatically.
NEXTCLOUD_TRUSTED_DOMAINS=localhost

# Optional: explicit public URL used by Nextcloud for generated links and redirects
NEXTCLOUD_OVERWRITE_CLI_URL=https://your-tunnel.example.com

# Optional: space-separated reverse proxies that are allowed to forward requests
NEXTCLOUD_TRUSTED_PROXIES=127.0.0.1 cloudflared

# MariaDB credentials
MYSQL_ROOT_PASSWORD=your_root_password
MYSQL_DATABASE=nextcloud
MYSQL_USER=nextcloud
MYSQL_PASSWORD=your_db_password

# Cloudflare Tunnel token (get from Cloudflare Zero Trust dashboard)
CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token
